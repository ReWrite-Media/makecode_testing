/**
 * Custom blocks
 */
namespace makecodeTest {
    /**
     * count to 20
     */
    //% block="count to 20" blockId=agentdropallitemsindividually
    export function count_to_20() {
        for (let i = 0; i < 0; i++) {
            player.execute("say "+i);
        }
    }
}